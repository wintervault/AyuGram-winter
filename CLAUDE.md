# Claude Code Pointer

Read `AGENTS.md` and treat it as the canonical repository-wide instructions.

This is a personal fork (`wintervault/AyuGram-winter`). Never interact with
upstream projects (`AyuGram/AyuGramDesktop`, `telegramdesktop/tdesktop`) in
any write way — no pushes, PRs, or issues there. Submodule fetches are the
only permitted read-only upstream access.

For ongoing work state, plans, and rules, read `.ai-workspace/CONTEXT.md`,
`.ai-workspace/PLAN.md`, and `.ai-workspace/WORKLOG.md` first (local only,
gitignored). Update them at the end of each session.
